{
  "name": "calculadora-sem-nocao",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:apk": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "latest",
    "@capacitor/android": "latest",
    "@capacitor/cli": "latest"
  }
}
